{
  "name": "omarchy-loose-ends-entry",
  "version": "0.1.0",
  "private": true,
  "description": "Local-only Git loose-end queue for the Omarchy bar",
  "scripts": {
    "test": "bash -n bin/loose-ends-scan && c8 --all --include=Model.js --check-coverage --lines 95 --functions 95 --branches 90 --statements 95 node --test tests/*.test.js",
    "test:smoke": "node --test tests/smoke.test.js",
    "test:race": "for run in 1 2 3; do node --test tests/scanner.test.js || exit; done",
    "test:mutation": "stryker run",
    "test:e2e": "bash e2e/buzz.sh",
    "audit": "audit-harness verify && audit-harness audit . --deep && audit-harness scan ."
  },
  "devDependencies": {
    "@intentsolutions/audit-harness": "1.4.0",
    "@stryker-mutator/core": "^10.0.0",
    "c8": "^12.0.0"
  },
  "overrides": {
    "qs": "6.15.3"
  }
}
