{
  "name": "omarchy-loose-ends-entry",
  "version": "0.1.0",
  "private": true,
  "description": "Local-only Git loose-end queue for the Omarchy bar",
  "scripts": {
    "test": "bash -n bin/loose-ends-scan && c8 --all --include=Model.js --check-coverage --lines 95 --functions 95 --branches 90 node --test tests/*.test.js"
  },
  "devDependencies": {
    "c8": "^12.0.0"
  }
}
